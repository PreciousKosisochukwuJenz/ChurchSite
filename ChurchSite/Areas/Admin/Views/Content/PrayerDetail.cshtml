@model ChurchSite.Areas.Admin.ViewModels.PrayerVM
@{
    ViewBag.Title = "Prayer detail";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    //string ImageBase64 = Convert.ToBase64String(Model.Image);
    //string ImageSrc = string.Format("data:image/gif;base64,{0}", ImageBase64);
}

<div class="app-main__inner">
    <div class="main-card mb-3 card">
        <div class="card-body">
            <img src="@Model.FilePath" style="width: 100%;height:100%;" class="d-block text-center" />
            <h2 class="text-center">@Model.Title</h2>
            <p class="text-center">@Model.Description</p><hr /><br />
            @Html.Raw(Model.Body)
        </div>
    </div>
</div>
